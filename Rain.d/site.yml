site:
  $$nodup: true

  session:
    backend: redis
    options:
      struct_id: luohua.session
      cookie-key: lh_s
      secret: '7h1z_must_13e_s3cr37'
      ttl: 300

  staticfile:
    root: ../jnrain2/static/

  middlewares:
    pre:
      - session
      - luohua.metrics
    post:
      - luohua.metrics
      - session

  views:
    package: luohua.app
    modules:
      # API 界面
      - .v1.session
      - .v1.account
      - .v1.vpool
      - .v1.vtag
      - .v1.vthread
      - .v1.vfile

      # 杂项 API
      - .misc

      # 中间件
      - .metrics.middleware

      # 实时信道
      - .rt
      - luohua.rt.ns


# vim:set ai et ts=2 sw=2 sts=2 fenc=utf=8:
